import React from "react";

/** HR module salary inputs — placeholder until HR payroll inputs are built. */
export default function HrSalaryInputs() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[360px] rounded-lg border border-dashed border-slate-300 bg-slate-50/80 text-center px-6">
      <h2 className="text-base font-semibold text-gray-900">Salary Inputs</h2>
      <p className="text-sm text-gray-500 mt-2 max-w-md">
        This section is reserved for HR salary inputs. Content will be added here later.
      </p>
    </div>
  );
}
